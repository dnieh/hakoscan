{
  "name": "hakoscan",
  "version": "1.0.0",
  "description": "Local Nissan Consult I dashboard",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "mock": "MOCK=1 node server.js",
    "build": "npm --prefix client install && npm --prefix client run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "serialport": "^13.0.0"
  }
}
